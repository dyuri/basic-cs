# List implementations

## Basic `List`

... as *interface*:
- `add(d)`
- `insert_before(idx, d)`
- `get(idx)`
- `set(idx, d)`
- `index_of(d)`
- `remove(idx)`
- `clear()`
- `reversed()`
- `size`

## Variants

- `LinkedList`
- TODO functional
- TODO arraylist
